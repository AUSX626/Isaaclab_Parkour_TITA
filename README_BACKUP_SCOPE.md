# Workstation code backup

This repository is a code-only backup of `/home/kemove/zhr_data` for the TITA / IsaacLab parkour work.

Included top-level code folders:

- `instinctlab/`
- `instinct_rl/`
- `IsaacLab/`
- `Isaaclab_Parkour/`
- `project-instinct-tita/`

Excluded intentionally:

- datasets and model data
- training logs and outputs
- checkpoints and exported policies
- Python caches and egg-info
- nested `.git` histories and editor caches
